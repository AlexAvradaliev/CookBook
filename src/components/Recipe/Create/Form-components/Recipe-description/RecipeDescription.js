import styles from './RecipeDescription.module.css';

const RecipeDescription = (props) => {

    return (
        <textarea placeholder="Recipe description" rows="4" />
    );
};

export default RecipeDescription;
